# 客运公司模拟器：项目级强制规则

## 1. 版本替换纪律

1. 本仓库从 0 开发，不迁移任何旧仓库运行代码。
2. 每次版本升级，废弃实现、废弃入口、废弃接口必须先从当前代码树删除，再接入替代实现。
3. 禁止 V1/V2、old/new、legacy/new、backup/new 两套正式实现并存。
4. 禁止通过长期覆盖补丁保留旧逻辑。
5. 正式公共能力只能有一个入口。
6. 每次新增、替换、删除正式功能必须更新 CHANGELOG.md。
7. 禁止临时占位代码冒充正式实现。

## 2. 依赖方向

允许：

表现层 -> 应用层 -> 规则核心 <- 数据适配层

禁止：

- 规则核心依赖 DOM、Android API、广告 SDK、UI 框架或图片资源。
- UI 直接修改现金、线路、车辆、客流、时间。
- 数据文件调用 UI。
- 为单一城市、单一车型写专用业务分支。

## 3. 基础单位

规则层统一使用整数：

- 金额：分
- 距离：米
- 世界坐标：米
- 时间：游戏秒
- 速度：米/秒
- 比例值：千分数

显示层负责换算成人民币、公里、小时、百分比。

## 4. 大地图规则

1. 全国、区域、城市、站场只是不同缩放级别，不是四套地图逻辑。
2. 车辆位置必须由正式道路、线路、所在路段和路段偏移计算。
3. UI 禁止维护独立的“车辆动画位置”。
4. 地图视觉比例可以压缩，但模拟里程必须读取正式道路长度。
5. 城市、道路、站点、线路、车型不得写死在 UI 中。

## 5. 存档

1. 存档必须包含 saveVersion。
2. 已发布版本修改存档结构必须增加 saveVersion。
3. 已发布版本需要逐版本迁移，不允许用“删档重开”解决兼容性。
4. 开发期测试存档如被重置，必须在 CHANGELOG 明确说明。

## 6. 提交要求

进入 main 前必须通过：

- npm run typecheck
- npm test
- npm run check

任何重复接口、旧入口残留、失败测试都不得作为正式版本保留。
